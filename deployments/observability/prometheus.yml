global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: durableflow-api
    static_configs:
      - targets: ["api:8080"]

  - job_name: durableflow-worker
    static_configs:
      - targets: ["worker:8081"]
